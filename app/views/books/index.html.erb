
<table>

    <tr>
        <th>
            Tilte
        </th>
        <th>
            Body
        </th>
    </tr>


    <% @books.each do |book| %>
        
        <tr>
            <td>
                <%= book.title %>
            </td>
            <td>
                <%= book.body %>
            </td>
        </tr>

    <% end %>

</table>

<p>New Book</p>

<%= form_with model: @book do |f| %>
    <%= f.text_field :title %>
    <%= f.text_field :body %>
    <%= f.submit 'create' %>
<% end %>

