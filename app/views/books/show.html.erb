<h1>Books#show</h1>
<p>Find me in app/views/books/show.html.erb</p>

<%= render 'users/info', user: @user, book: Book.new %>


<table>

    <tr>
        <th>
            Book detail
        </th>
        <th>
        </th>
        <th>
        </th>
        <th>
        </th>
        <th>
        </th>
    </tr>


    
        
        <tr>
            <td>
                <%= link_to user_path(@user) do %>
                    <%= image_tag @user.get_profile_image(100,100) %><br>
                    <%= @user.name %>
                <% end %>
            </td>
            <td>
                <%= @book.title %>
            </td>
            <td>
                <%= @book.body %>
            </td>
            <td>
                <%= button_to edit_book_path(@book), method: :get do %>
                    edit
                <% end %>
            </td>
            <td>
                <%= button_to book_path(@book), method: :delete do %>
                    destroy
                <% end %>
            </td>
        </tr>

    

</table>
